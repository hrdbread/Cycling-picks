# Cookie session secret (32+ chars). Generate: openssl rand -base64 32
SESSION_SECRET=replace-me-with-a-long-random-string-please-32chars

# SQLite location
DATABASE_PATH=./data/cycling.db

# Optional override for procyclingstats base
PCS_BASE_URL=https://www.procyclingstats.com
